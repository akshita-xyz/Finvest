{
  "name": "finvest-blockchain",
  "private": true,
  "version": "1.0.0",
  "description": "Finvest certificate registry — keccak256 hashes of issued certificates pinned on-chain so anyone can verify authenticity.",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js",
    "node:chain": "hardhat node",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "env:sync": "node scripts/sync-frontend-env.js",
    "cert:auto-local": "node scripts/run-local-cert-stack.js",
    "cert:issue": "hardhat run scripts/issue-cert.js"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "ethers": "^5.7.2",
    "hardhat": "^2.22.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.6",
    "dotenv": "^16.4.5"
  }
}
