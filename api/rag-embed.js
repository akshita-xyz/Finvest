/**
 * Vercel serverless when Project Root Directory = repository root (see /vercel.json).
 * Delegates to the canonical handler in FRONTEND/api/rag-embed.js
 */
export { default } from '../FRONTEND/api/rag-embed.js';
