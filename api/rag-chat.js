/**
 * Vercel serverless when Project Root Directory = repository root (see /vercel.json).
 * Delegates to the canonical handler in FRONTEND/api/rag-chat.js
 */
export { default } from '../FRONTEND/api/rag-chat.js';
